<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light-mode only (decision: Visual Design System Source of Truth).
         Window theme used before Compose takes over; colors match the style guide. -->
    <style name="Theme.WorkerNotFound" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#FAF8F5</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">#FFFFFF</item>
        <item name="android:windowBackground">#FAF8F5</item>
    </style>
</resources>
